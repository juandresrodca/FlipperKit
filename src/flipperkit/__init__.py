"""FlipperKit — a companion CLI toolkit for the Flipper Zero.

Backup the SD card, parse captured artifacts (.nfc / .sub / .rfid / .ir / …),
index them into SQLite and generate shareable reports.
"""

__version__ = "0.1.0"
